function Footer() {
    return ( <footer>
        <nav>
            <h2>{"<Footer>"}</h2>
            <p>{new Date().toLocaleTimeString()}</p>
        </nav>
    </footer> );
}

export default Footer;